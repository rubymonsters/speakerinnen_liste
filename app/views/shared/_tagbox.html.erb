<% tags.each do |tag| %>
  <button id="<%= tag.name.gsub(/\s+/, "-") %>" class="category_<%= category.short_name %> bg-<%= category.short_name %> tag-button btn m-1 available-tag"><%= tag.name %></button>
<% end %>
