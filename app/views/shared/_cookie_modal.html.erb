<!-- Modal Cookie -->
<div class="modal fade" id="contactCookie" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="ModalLabel">
          <%= t(:hint, scope: 'pages.home' ) %>
        </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p><%= t(:cookie_contact, scope: 'pages.home', link: privacy_path).html_safe %>
        <%= link_to(
          t(:allow_cookies, scope: 'pages.home'),
          url_for(params.permit!.merge(allow_cookies: true)),
          class: 'mt-3 btn btn-blue'
        ) %>
        </p>
      </div>
    </div>
  </div>
</div><!-- Modal Cookies-->
