<% @categories.each_slice(5).with_index do |sliced_categories, index| %>
  <% if index==0 %>
    <div class="layout__item 1/2">
  <% else %>
    --><div class="layout__item 1/2">
  <% end %>
    <ul class="page-categories__list__no-bullets">
      <% sliced_categories.each do |category| %>
        <li>
          <%= category_link category, class:"page-categories__list__links" %>
        </li>
      <% end %>
    </ul>
  <% if index==9 %>
    </div>
  <% else %>
    </div><!--
  <% end %>
<% end %>-->
