<section class="bg--grey">
  <div class="layout-wrapper">
    <div class="o-layout layout">
      <div class="o-layout__item 3/3 palm-1/1">
        <% if @profile.present? %>
        <h2 class="bb"><%= "#{t(:contact, scope: 'profiles.show' )} #{@profile.fullname}:"%></h2>
        <% end %>
        <%= render partial: 'form', locals: { url: '' } %>
      </div>
    </div>
  </div>
</section>
