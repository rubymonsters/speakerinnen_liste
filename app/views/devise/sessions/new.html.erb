<% @target_url = session_path(resource_name)
   @signup = false
   %>
<%= render 'devise/shared/auth', locals: {
    signup: @signup,
    resource: @resource,
    resource_name: @resource_name,
    target_url: @target_url
    } %>
