<div class="container profiles">
  <h1 class="title-profiles">Medialinks</h1>
    <div class="row">
      <div class="span2">
        <%= link_to '<i class="icon-plus icon-white"></i> Hinzufügen'.html_safe,
                  new_profile_medialink_path(@profile), class: "btn btn-success" %>
        <br>
        <br>
      </div>
      <div class="span10">
      </div>
    </div>
    <div class="row">
      <div class="span12">
        <table class="table table-striped table-hovered">
          <tr>
            <th>Link</th>
            <th>Title</th>
            <th>Description</th>
            <th></th>
            <th></th>
          </tr>
          <% @medialinks.each do |medialink| %>
            <tr>
              <td><%= link_to medialink.url, medialink.url %></td>
              <td><%= medialink.title %></td>
              <td><%= medialink.description %></td>
              <td><%= link_to '<i class="icon-pencil"></i> edit'.html_safe, edit_profile_medialink_path(@profile.id, medialink.id) %></td>
              <td>
                <%= link_to '<i class="icon-remove"></i> delete'.html_safe,
                            profile_medialink_path(@profile.id, medialink.id),
                            confirm: 'Are you sure?',
                            method: :delete %>
              </td>
            </tr>
          <% end %>
        </table>
        <br />
      </div>
      <hr/>
  </div>
</div>
